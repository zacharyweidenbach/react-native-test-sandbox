export type Item = {
  id: string;
  teamColor: string;
  createdAt: string;
  firstName: string;
  lastName: string;
};

export type PlayerListStack = {
  PlayerList: undefined;
  PlayerDetails: { id: string };
};
