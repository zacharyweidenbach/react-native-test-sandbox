export type ConventionalStackList = {
  ConventionalList: undefined;
  ConventionalDetails: { id: string };
};

export type Item = {
  id: string;
  teamColor: string;
  createdAt: string;
  firstName: string;
  lastName: string;
};

export type FSMStackList = {
  FSMList: undefined;
  FSMDetails: { id: string };
};
